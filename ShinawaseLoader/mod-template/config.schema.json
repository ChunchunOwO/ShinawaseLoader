{
  "type": "object",
  "properties": {
    "enabledFeature": {
      "type": "boolean",
      "title": "Enabled feature"
    },
    "message": {
      "type": "string",
      "title": "Message",
      "description": "Shown by the sample mod."
    },
    "accent": {
      "type": "string",
      "title": "Accent",
      "description": "Color used by the sample badge.",
      "enum": ["auto", "violet", "teal"],
      "default": "auto"
    }
  }
}
