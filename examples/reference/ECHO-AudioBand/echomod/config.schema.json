{
  "type": "object",
  "properties": {
    "locale": {
      "type": "string",
      "title": "界面语言 / UI language",
      "description": "auto 跟随系统；zh-CN 或 en-US 强制语言。",
      "enum": ["auto", "zh-CN", "en-US"],
      "default": "auto"
    },
    "widgetWidth": {
      "type": "integer",
      "title": "组件宽度 / Widget width",
      "description": "任务栏条在 100% 缩放下的宽度（像素）。",
      "minimum": 200,
      "maximum": 800,
      "default": 360
    },
    "uiScale": {
      "type": "integer",
      "title": "界面缩放 / UI scale",
      "description": "50–200，默认 100。按比例缩放条宽、字号、封面、按钮和悬停预览；高度仍贴任务栏再按比例变化。",
      "minimum": 50,
      "maximum": 200,
      "default": 100
    },
    "alignment": {
      "type": "string",
      "title": "水平对齐 / Alignment",
      "description": "相对任务栏空档：靠左、居中或靠右。会读取系统任务栏左/中对齐，并避开托盘图标。 / Sits in the free taskbar gap. Follows Windows left/center taskbar alignment and stays off the tray.",
      "enum": ["left", "center", "right"],
      "default": "right"
    },
    "offsetX": {
      "type": "integer",
      "title": "水平偏移 / Horizontal offset",
      "description": "靠左时从空档左缘再内缩；靠右且关闭自动避开托盘时作为右边距。 / Extra inset from the left free edge; right margin only when auto-avoid tray is off.",
      "minimum": 0,
      "maximum": 600,
      "default": 12
    },
    "offsetY": {
      "type": "integer",
      "title": "垂直偏移 / Vertical offset",
      "description": "相对任务栏条的额外垂直偏移。",
      "minimum": -80,
      "maximum": 80,
      "default": 0
    },
    "monitor": {
      "type": "string",
      "title": "显示器 / Monitor",
      "description": "primary 为主屏，或显示器索引（0 起）。",
      "default": "primary"
    },
    "customHeight": {
      "type": "integer",
      "title": "回退高度 / Fallback height",
      "description": "垂直任务栏或自动隐藏时，浮动条的高度。",
      "minimum": 28,
      "maximum": 80,
      "default": 48
    },
    "showAlbumArt": {
      "type": "boolean",
      "title": "显示封面 / Show album art",
      "default": true
    },
    "showControls": {
      "type": "boolean",
      "title": "显示控制按钮 / Show controls",
      "default": true
    },
    "showProgress": {
      "type": "boolean",
      "title": "显示进度条 / Show progress",
      "default": true
    },
    "showTime": {
      "type": "boolean",
      "title": "显示时间 / Show time",
      "description": "在艺人行右侧显示 mm:ss / mm:ss。",
      "default": false
    },
    "theme": {
      "type": "string",
      "title": "主题 / Theme",
      "description": "组件跟随系统深浅色（auto）；或强制深色 / 浅色。 / auto follows the Windows taskbar light/dark theme.",
      "enum": ["auto", "dark", "light"],
      "default": "auto"
    },
    "accentColor": {
      "type": "string",
      "title": "强调色 / Accent color",
      "description": "进度条与按钮悬停颜色。",
      "default": "#4da3ff"
    },
    "backgroundOpacity": {
      "type": "integer",
      "title": "背景不透明度 / Background opacity",
      "description": "0–100。",
      "minimum": 0,
      "maximum": 100,
      "default": 88
    },
    "scrollingText": {
      "type": "boolean",
      "title": "滚动标题 / Scrolling text",
      "description": "标题或艺人溢出时使用跑马灯。",
      "default": true
    },
    "autoHideWhenStopped": {
      "type": "boolean",
      "title": "停止时自动隐藏 / Auto-hide when stopped",
      "description": "停止且无标题连续超过 8 秒后隐藏组件。",
      "default": false
    },
    "pollIntervalMs": {
      "type": "integer",
      "title": "轮询间隔 / Poll interval (ms)",
      "description": "从 ECHO 读取播放状态的间隔。",
      "minimum": 250,
      "maximum": 5000,
      "default": 1000
    },
    "autoAvoidTray": {
      "type": "boolean",
      "title": "自动对齐托盘 / Auto-align to tray",
      "description": "所有对齐方式都不会压到系统托盘图标；靠右时贴在托盘左侧。 / Keep every alignment clear of tray icons; right alignment sits just left of the tray.",
      "default": true
    },
    "seamlessMode": {
      "type": "boolean",
      "title": "无缝融合任务栏 / Seamless taskbar look",
      "description": "直角、不透明、填满任务栏条，视觉上如同嵌入任务栏（不改变窗口层级）。 / Square, opaque, fills the taskbar strip for an embedded look without reparenting.",
      "default": false
    },
    "hoverPreview": {
      "type": "boolean",
      "title": "悬停封面预览 / Hover album-art preview",
      "description": "鼠标悬停封面时弹出大图与曲目信息。 / Show a large cover popup when hovering album art.",
      "default": true
    },
    "backdrop": {
      "type": "string",
      "title": "窗口材质 / Backdrop",
      "description": "WinUI 3 系统材质。Win11 默认 Mica；Win10 使用纯色，避免玻璃材质把整条打成全透明。none 为纯色。 / WinUI 3 system backdrop. Mica on Windows 11; solid fill on Windows 10.",
      "enum": ["mica", "acrylic", "tabbed", "none"],
      "default": "mica"
    },
    "hideWhenFullscreen": {
      "type": "boolean",
      "title": "全屏时隐藏 / Hide when fullscreen",
      "description": "游戏独占全屏、无边框铺满监视器、或 D3D 全屏时隐藏组件。 / Hide during exclusive / D3D / borderless-monitor fullscreen.",
      "default": true
    },
    "hideWhenPresentation": {
      "type": "boolean",
      "title": "演示模式隐藏 / Hide during presentation",
      "description": "Windows 演示 / 投影模式时隐藏。 / Hide when Windows reports presentation mode.",
      "default": true
    }
  }
}
